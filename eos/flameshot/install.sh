#!/usr/bin/env bash
# flameshot: screenshot tool with annotation, handles our mixed-orientation
# multi-monitor layout correctly (unlike ksnip, which has an unfixed upstream
# bug misaligning the capture region across monitors of different orientation)
sudo pacman -S --needed --noconfirm flameshot
